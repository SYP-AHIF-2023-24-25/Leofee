export class Student {
    firstName: string;
    lastName: string;
    className: string;
    constructor(firstName: string, lastName: string, className: string) {
        this.firstName = firstName;
        this.lastName = lastName;
        this.className = className;
    }
}

